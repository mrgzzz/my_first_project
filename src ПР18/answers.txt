1. NavLink позволяет применять стили к активной ссылке, а Link — нет.
2. С помощью хука useParams из React Router.
3. Это переход между маршрутами с помощью JavaScript, реализуется через хук useNavigate.
4. Через состояние (state) в навигации или через параметры URL.
5. Routes объявляет группу маршрутов, внутри которой отображается только один подходящий Route.
6. Создать компонент, который проверяет условие (например, авторизацию) и возвращает либо защищаемый компонент, либо редирект.
7. useNavigate — хук для программного перехода, redirect — компонент для перенаправления.